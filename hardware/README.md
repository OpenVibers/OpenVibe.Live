# Moved: hardware integration

The Raspberry Pi / single-board-computer scripts (`streamer.py`, `controller.py` and their guide)
now live in [OpenVibe.Extensions `hardware/`](https://github.com/OpenVibers/OpenVibe.Extensions/tree/main/hardware),
the home of OpenVibe's first-party client surfaces (ADR-023). They moved unchanged; their history
before the move is in this repository's git log under `hardware/`.
