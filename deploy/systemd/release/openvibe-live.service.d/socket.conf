# Bind the service to the socket unit that owns its listener. Installed as a drop-in so the main
# unit stays usable on a box that has not been switched over to socket activation.
[Unit]
Requires=openvibe-live.socket
After=openvibe-live.socket
